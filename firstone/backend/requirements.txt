# FastAPI et serveur ASGI
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# WebSocket support
websockets>=13.1

# CORS et sécurité
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.1.1  # Mis à jour pour compatibilité avec CrewAI

# Validation de données
pydantic>=2.11.9  # Mis à jour pour compatibilité avec CrewAI
pydantic-settings>=2.10.1  # Mis à jour pour compatibilité avec CrewAI

# Intégration avec CrewAI (depuis votre projet)
crewai[tools]==1.4.1

# LangChain pour knowledge ingestion (versions compatibles avec CrewAI)
langchain>=0.3.0
langchain-community>=0.3.0
pypdf>=5.1.0
python-docx>=1.1.2

# Base de données (optionnel - version compatible)
sqlalchemy>=1.4,<2.0.36
databases[postgresql]>=0.9.0

# HTTP client
httpx>=0.27.2
aiofiles>=24.1.0

# Logging et monitoring
loguru>=0.7.2
